#include<iostream>
using namespace std;

//declaration
int findMax(int x, int y, int z);

int main() {

  //function call
	cout << findMax(2, 632, 2949);


}
int findMax(int x, int y, int z) {
	if (x > y && x > z)
		return x;
	else if (y > x && y > z)
		return y;
	else
		return z;
}
